# P1 视觉性能观测

门禁满足后，在 dev 端口 `5205` 分档实测并分别记录。

## 桌面档

目标 60 FPS，资源包 2–50 MB，三角形不超过 500,000，draw call 不超过 200。

## 移动档

目标 30 FPS，资源包 2–5 MB，三角形不超过 200,000，draw call 不超过 100。移动档与桌面档独立记录。

`perfObserver.js` 每秒采样 FPS、drawCalls、triangles。当前门禁未满足，暂无实测数据。

## 真机核对清单

- [ ] 5205 dev 页面可在 iOS Safari 与 Android Chrome 打开。
- [ ] 移动档持续达到 30 FPS，三角形 ≤200,000，draw call <100。
- [ ] 资源包大小处于 2–5 MB。
- [ ] 点选热区为 44–48px。
- [ ] 阅读面板滚动时地图不旋转。
- [ ] 低动作成本替代表可用。
- [ ] 雾、渐变天空和后处理没有明显闪烁或发热。
- [ ] 七回归用例均通过并留存截图。

## 5205 收口核对（2026-09-17）

HTTP 资源核对通过：页面、B流 meshopt GLB、manifest 均返回 200。当前环境无可用浏览器自动化后端，FPS/drawCalls/triangles 未采样。

