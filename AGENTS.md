# AGENTS.md — 项目约定

## 全局

- 用中文交流与汇报；代码标识符、变量、函数名用英文。
- Windows 环境：一律用 npm.cmd 而非 npm；PowerShell 执行策略已设为 RemoteSigned。
- 网络：系统/终端代理 127.0.0.1:7897，localhost/127.*/100.*（Tailscale）直连，不要改这些设置。
- 修改范围最小化：不顺手重构、不引入新依赖（three 与 vite 之外需先说明）、不主动删除文件。

## 项目结构

- src/core/ — Three.js 引擎、相机、材质、程序化几何、世界构建、特效。
- src/game/ — 游戏状态机、问题包内容数据、Bloomy Agent、引导逻辑。
- src/ui/ — HTML/CSS 界面层：提问、卡片、对照、成形、HUD。
- 美术方向：风格化微缩立体世界（stylized diorama），高对比涂鸦配色，克制的粒子与发光。

## 验证命令（改完必须执行）

    npm.cmd run build

构建必须零错误通过。涉及运行时交互改动时，另起 npm.cmd run dev 并在浏览器实际操作核心链路：提问 → 世界生成 → 探索收集 → 对照 → 成形。

## 内容约定

- Bloomy 的台词必须温和、不裁决、不替用户下结论；不能替真实人物编造经历之外的断言。
- 奖励围绕认知动作（经历/条件/分歧/共鸣/盲点/整合），不做积分、排行榜、刷任务式游戏化。
